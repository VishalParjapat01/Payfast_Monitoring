name: Payment gateway monitor

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
    inputs:
      simulate_failure:
        description: 'Simulate a payment failure for alert testing'
        required: true
        default: false
        type: boolean

jobs:
  check-payment-page:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - name: Check monitor secrets
        shell: bash
        env:
          USER_EMAIL: ${{ secrets.USER_EMAIL }}
          USER_PASSWORD: ${{ secrets.USER_PASSWORD }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_FROM: ${{ secrets.ALERT_FROM }}
          ALERT_TO: ${{ secrets.ALERT_TO }}
        run: |
          for variable in USER_EMAIL USER_PASSWORD SMTP_HOST SMTP_USER SMTP_PASSWORD ALERT_FROM ALERT_TO; do
            if [ -z "${!variable}" ]; then
              echo "Missing GitHub Actions secret: $variable"
              exit 1
            fi
          done
      - name: Restore monitor state
        uses: actions/cache/restore@v4
        with:
          path: .payment-monitor-state.json
          key: payment-monitor-state-${{ github.run_id }}
          restore-keys: payment-monitor-state-
      - name: Run ticket-booking payment check
        run: xvfb-run --auto-servernum npm run monitor:payment
        env:
          HEADLESS: 'false'
          MONITOR_TEST_MODE: ${{ github.event_name == 'workflow_dispatch' && 'true' || 'false' }}
          SIMULATE_FAILURE: ${{ github.event_name == 'workflow_dispatch' && inputs.simulate_failure || 'false' }}
          BASE_URL: https://mindmatters-summit.com
          USER_EMAIL: ${{ secrets.USER_EMAIL }}
          USER_PASSWORD: ${{ secrets.USER_PASSWORD }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_SECURE: ${{ secrets.SMTP_SECURE }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_FROM: ${{ secrets.ALERT_FROM }}
          ALERT_TO: ${{ secrets.ALERT_TO }}
      - name: Save monitor state
        if: always()
        uses: actions/cache/save@v4
        with:
          path: .payment-monitor-state.json
          key: payment-monitor-state-${{ github.run_id }}